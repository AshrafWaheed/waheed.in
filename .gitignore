# Laravel
backend/.env
backend/ADMIN_CREDENTIALS.txt
# Google OAuth client download — the secret lives in backend/.env, never here.
backend/client_secret_*.json
backend/vendor/
backend/node_modules/
backend/storage/logs/*
backend/storage/backups/
backend/.phpunit.result.cache

# Next.js
frontend/.env
frontend/.env.local
frontend/node_modules/
frontend/.next/
frontend/data/

# Docs / planning (*.md files; CLAUDE.md exempt as already-tracked)
*.md
# All planning docs live here. Ignored wholesale so non-.md assets
# (diagrams, exports) dropped alongside them stay untracked too.
documents/

# General
.DS_Store
Waheed-handoff.zip
